apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    isDefault: true
    access: proxy
    # "prometheus" resolves to the Prometheus container via Docker DNS.
    # Never use localhost here — see docs/troubleshooting.md section 4.
    url: http://prometheus:9090
    editable: true
